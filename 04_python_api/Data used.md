* Used sample data from the free jsonplaceholder api and created the dummy data using chatgpt and claude.
* For API creation i have used the fastapi for api creation and use uvicorn a lightweighted web server to run this fastapi.
* Data is store in the postgresql development data that is already present inside the replit.
